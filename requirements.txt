# ============================================================
# Content Moderation POC v0.5.0 — Python Dependencies
# ============================================================

# Agent Orchestration
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0

# API Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
python-multipart>=0.0.9

# LLM API (DeepSeek / OpenAI)
langchain-openai>=0.2.0
openai>=1.0.0

# LLM API (Anthropic — optional)
langchain-anthropic>=0.3.0

# Local LLM — llama.cpp (activate with LLM_PROVIDER=local)
# llama-cpp-python  ← uncomment if using llama.cpp local LLM

# Local LLM — HuggingFace transformers (activate with LLM_PROVIDER=transformers)
# Uses model.generate(), supports any CausalLM model (Qwen2.5, Llama-3, etc.)
# bitsandbytes>=0.45.0  ← uncomment for 4-bit quantization (recommended)

# Local LLM — SGLang (activate with LLM_PROVIDER=sglang)
# RadixAttention prefix caching + structured JSON output + continuous batching
# sglang[all]>=0.3.0  ← uncomment if using SGLang backend

# BERT Inference
transformers>=4.45.0
torch>=2.4.0
accelerate>=0.20.0
sentencepiece>=0.2.0

# Embedding
sentence-transformers>=3.0.0

# Keyword Matching
pyahocorasick>=2.1.0
jieba>=0.42.0

# Vector Cache
chromadb>=0.5.0

# Memory Cache
cachetools>=5.0.0

# Redis Cache (optional — graceful degradation if unavailable)
redis>=4.0.0
hiredis>=2.0.0

# Image Processing
pillow>=10.0.0

# Image OCR (optional — only needed for image moderation)
# easyocr>=1.7.0

# HTTP
httpx>=0.27.0

# Config
python-dotenv>=1.0.0
